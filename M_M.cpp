#include <bits/stdc++.h>
using namespace std;
#define int long long
#define m 998244353
int32_t main() {
    cout << "wOKeeee";
    // int n;
    // cin >> n;

    // vector<int> a(n), b(n);
    // for (auto &i : a)
    //     cin >> i;
    // for (auto &i : b)
    //     cin >> i;
    // for (auto i : a)
    // //     cout << i << " ";
    // // // return 0;
    // int x = 30, y = 29, temp = 30;
    // for (int i = 0; i < y; i++) {
    //     // cout<<(x*x)
    //     temp = (temp * x) % m;
    // }
    // cout << temp%m;
    // int temp = 0;
    // for (int i = 0; i < n; i++) {
    //     temp += (a[i] % m);
    //     temp = (temp % m);
    //     cout << (temp % m);
    //     if (i < (n - 1)) {
    //         cout << " ";
    //     }
    // }
    cout << endl;
}