#include <bits/stdc++.h>
using namespace std;
#define int long long
signed main()
{
    int n, x;
    cin >> n >> x;
    map<int, int> mp;
    vector<int> v(n);
    int cnt = 0;
    for (auto &i : v)
        cin >> i;
    for (int i = 0; i < n; i++)
    {
        
    }

    cout << "IMPOSSIBLE";
}