#include <bits/stdc++.h>
using namespace std;
#define int long long
#define flash                         \
    ios_base::sync_with_stdio(false); \
    cin.tie(NULL);                    \
    cout.tie(NULL);

signed main()
{
    flash;
    int T;
    cin >> T;
    while (T--)
    {
        int a;
        cin >> a;
        cout << a - 1 << endl;
    }
}