#include <stdio.h>
int main()
{

    int n;
    scanf("%d", &n);
    while (n--)
    {

        int a;
        scanf("%d", &a);
        for (int i = 0; i < a; i++)
        {
            printf("%d ", 1);
        }

        printf("\n");
    }
}