#include <bits/stdc++.h>
using namespace std;

int main(){
    int n, m;
    cin >> n >> m;

    int temp = ceil(n / 4.0);
    for (int i = 0; i < temp; ++i)
    {
        cout << 5;
    } cout << endl;
    for (int i = 0; i < temp - 1; ++i)
    {
        cout << 4;
    } cout << 5;
    
}
