#include <bits/stdc++.h>
using namespace std;
#define flash                         \
    ios_base::sync_with_stdio(false); \
    cin.tie(NULL);
using namespace std;

void solve();

int main()
{
    flash;

    int T;

    while (cin >> T)
    {
        if (T == 0)
            break;
        T <<= 1;
vector<int,vector<int,int>> v;

        while (T--)
        {
            string s;
            int a, b;
            cin >> s >> a >> b;
            // cout << s << " " << a << " " << b;
        }
    }

    return 0;
}
