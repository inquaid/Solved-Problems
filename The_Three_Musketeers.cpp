#include <bits/stdc++.h>
using namespace std;

int check(int c) {
    return (c == 'a' or c == 'e' or c == 'i' or c == 'o' or c == 'u');
}

int main() {
    int t;
    cin >> t;
    while (t--) {
        int n;
        cin >> n;
        string s;

        for (int i = 0; i < n; i++)
        {
            cin >> s;
            for(auto c : s){
                switch (c)
                {
                case 'a':
                    
                    break;
                
                default:
                    break;
                }
            }
        }
        
    }
}