#include <bits/stdc++.h>
using namespace std;
static int t = -1;
int main()
{
    if (t == -1)
        cin >> t;

    int n;
    cin >> n;
    

    t--;
    if (t == 0)
        return 0;
    main();
}